# Panel administrativo

Este panel es una maqueta local: usa `localStorage` y no incluye autenticación ni base de datos. No debe exponerse públicamente hasta integrar inicio de sesión, permisos por rol, persistencia de datos y cierre de sesión en servidor.
